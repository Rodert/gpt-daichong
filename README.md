# AI 订阅指南

一个面向中文用户的 ChatGPT 订阅方式说明站。站点使用 Jekyll，可部署到 GitHub Pages。

网站内容在 [index.md](./index.md)，推荐服务链接通过 [_config.yml](./_config.yml) 统一配置。

## 本地预览

在已安装 Ruby、Bundler 与 Jekyll 的环境中运行：

```bash
bundle exec jekyll serve
```

随后访问 `http://localhost:4000`。
